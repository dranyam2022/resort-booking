import React from "react";
import styles from "./HeroCard.module.css";

const HeroCard = () => {
  return (
    <main className={styles.container}>
      <h1 className={styles.title}>STAY WITH COMFORT</h1>
    </main>
  );
};

export default HeroCard;
